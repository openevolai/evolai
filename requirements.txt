# EvolAI — pip requirements (mirrors [project.dependencies] in pyproject.toml)
#
# Prefer:  pip install -e .          (reads pyproject.toml directly)
# Or:      pip install -r requirements.txt
#
# NOTE: vllm is NOT listed here.  It must live in a SEPARATE virtualenv
# because bittensor pins fastapi<0.111 / setuptools<71 while vllm needs
# fastapi>=0.115 / setuptools>=77.  The validator calls `vllm serve` as a
# subprocess — no Python import required.
#
# Quick setup (creates both envs automatically):
#   bash scripts/setup-validator.sh
#
# ──────────────────────────────────────────────────────────────────────────────

typer>=0.12.0
rich>=13.7.0
httpx>=0.27.0
bittensor-wallet==4.0.1
bittensor==10.2.0
transformers>=5.3.0
torch>=2.0.0
bitsandbytes>=0.43.0; platform_system == "Linux"
accelerate>=0.20.0
huggingface_hub>=0.20.0
wandb>=0.16.0
python-dotenv>=1.0.0
datasets>=2.14.0
psutil>=5.9.0
openai>=1.0.0
aiohttp>=3.9.0
